ktor {
    deployment {
        port = 8080
        port = ${?PORT}

        watch = [ WikiApi ]
        autoreload = true
    }
    application {
        modules = [ ktor.ApplicationKt.module ]
    }
}
